<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todos</title>
</head>
<body>
    <h1>Todo List</h1>
    <ul>
        <% if(todos) { %>
            <% todos.forEach(item => { %>
                <li><%= item.todo %></li>
            <% }) %>
        <% } %>
    </ul>

    <form action="/todos" method="post">
        <label for="todo">Todo</label>
        <input id="todo-input" type="text" name="todo" placeholder="Enter Todo item">
        <button type="submit">Submit</button>
    </form>
   
</body>
</html>